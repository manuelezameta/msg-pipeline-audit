# =============================================================================
# application.yml — msg-pipeline-audit-sesion-05
# =============================================================================
spring:
  application:
    name: msg-pipeline-audit-sesion-05

app:
  aws:
    region: ${AWS_REGION_NAME:us-east-2}
    dynamodb-table: ${DYNAMODB_TABLE_NAME:msg-pipeline-messages}
    sns-topic-arn: ${SNS_TOPIC_ARN:}

  audit:
    processing-delay-seconds: 15
    ttl-days: 30

logging:
  level:
    com.msgpipeline: INFO
    software.amazon.awssdk: WARN
